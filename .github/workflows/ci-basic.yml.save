name: ci-Basic

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on:

    steps:
      -name: Checkout code
       uses: actions/checkout@v4
      -name: Run shellcheck
       run: |
        echo"Proveryaem skrypty"
	sudo apt-get update && sudo apt-get install -y shellcheck
        shellcheck scripts/*.sh || echo "Predypezdenya, no ne blokiruem"
